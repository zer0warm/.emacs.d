;; Use straight.el
;; Avoid `package-initialize` or `package-load` or `package-selected-packages`

(straight-use-package 'elfeed)
(straight-use-package 'org-journal)
(straight-use-package 'org-roam)
(straight-use-package 'evil)
